import { FormEvent,useState } from 'react';import { postJson } from '../lib/api';export default function ForgotPassword(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=await postJson<any>('/api/auth/request-password-reset',Object.fromEntries(new FormData(e.currentTarget)));setMsg(`${data.message}${data.resetToken?' Token dev: '+data.resetToken:''}`)}return <section className="panel narrow"><h1>Recuperar contraseña</h1><form onSubmit={submit}><input name="email" type="email" placeholder="Email" required/><button>Solicitar token</button></form>{msg&&<p className="success">{msg}</p>}</section>}

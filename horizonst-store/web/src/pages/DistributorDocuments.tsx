import { useEffect,useState } from 'react';import { api } from '../lib/api';
export default function DistributorDocuments(){const[docs,setDocs]=useState<any[]>([]);useEffect(()=>{api<any[]>('/api/distributor/documents').then(setDocs)},[]);return <section className="panel"><h1>Documentación</h1><p>Listado documental. La subida PDF completa queda como pendiente de Fase 5B.</p>{docs.length===0?<p className="muted">Sin documentos.</p>:docs.map(d=><div className="line" key={d.id}><span>{d.document_type}</span><b>{d.status}</b><small>{new Date(d.created_at).toLocaleDateString('es-ES')}</small></div>)}</section>}

import { FormEvent,useState } from 'react';import { postJson } from '../lib/api';import ErrorMessage from '../components/ErrorMessage';export default function ResetPassword(){const[msg,setMsg]=useState('');const[err,setErr]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await postJson('/api/auth/reset-password',Object.fromEntries(new FormData(e.currentTarget)));setMsg('Contraseña actualizada.');setErr('')}catch(x:any){setErr(x.message)}}return <section className="panel narrow"><h1>Restablecer contraseña</h1><form onSubmit={submit}><input name="token" placeholder="Token" required/><input name="password" type="password" minLength={10} placeholder="Nueva contraseña" required/><button>Actualizar</button></form>{msg&&<p className="success">{msg}</p>}<ErrorMessage message={err}/></section>}

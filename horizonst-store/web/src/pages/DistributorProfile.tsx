import { FormEvent,useEffect,useState } from 'react';import { Link } from 'react-router-dom';import { api,patchJson } from '../lib/api';
export default function DistributorProfile(){const[p,setP]=useState<any>();const[msg,setMsg]=useState('');useEffect(()=>{api<any>('/api/distributor/profile').then(d=>setP(d.profile))},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=await patchJson<any>('/api/distributor/profile',Object.fromEntries(new FormData(e.currentTarget)));setP(d.profile);setMsg('Perfil actualizado')}return <section className="panel"><h1>Portal distribuidor</h1><Link to="/distributor/documents">Documentos</Link>{p&&<><p>Estado validación: <b>{p.validation_status}</b></p><form className="grid" onSubmit={submit}>{['company_name','tax_id','billing_address','city','province','postal_code','country','website','contact_person'].map(f=><input key={f} name={f} placeholder={f} defaultValue={p[f]??''}/>) }<button>Guardar</button></form></>}{msg&&<p className="success">{msg}</p>}</section>}

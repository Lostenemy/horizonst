import { FormEvent, useState } from 'react';import { useNavigate } from 'react-router-dom';import ErrorMessage from '../components/ErrorMessage';import { postJson } from '../lib/api';import { Session, auth } from '../lib/auth';
export default function Login(){const[error,setError]=useState('');const nav=useNavigate();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');try{const body=Object.fromEntries(new FormData(e.currentTarget));const data=await postJson<Session>('/api/auth/login',body);auth.save(data);nav('/dashboard');}catch(err:any){setError(err.message)}}return <section className="panel narrow"><h1>Iniciar sesión</h1><form onSubmit={submit}><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Contraseña" required/><button>Entrar</button></form><ErrorMessage message={error}/><a href="/forgot-password">¿Has olvidado la contraseña?</a></section>}

import { FormEvent,useState } from 'react';import { postJson } from '../lib/api';import ErrorMessage from '../components/ErrorMessage';export default function VerifyEmail(){const[msg,setMsg]=useState('');const[err,setErr]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await postJson('/api/auth/verify-email',Object.fromEntries(new FormData(e.currentTarget)));setMsg('Email verificado. Ya puedes iniciar sesión.');setErr('')}catch(x:any){setErr(x.message)}}return <section className="panel narrow"><h1>Verificar email</h1><form onSubmit={submit}><input name="token" placeholder="Token" required/><button>Verificar</button></form>{msg&&<p className="success">{msg}</p>}<ErrorMessage message={err}/></section>}
